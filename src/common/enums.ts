export enum EventType {
  Virtual = "Virtual",
  InPerson = "In-Person",
  Both = "Both",
  Unknown = "Unknown",
}

export enum PoapType {
  Poap = "Poap",
  Soulbound = "Soulbound",
  Consensual = "Consensual",
}

export enum Approved {
  Pending = "Pending",
  Approved = "Approved",
  Rejected = "Rejected",
}